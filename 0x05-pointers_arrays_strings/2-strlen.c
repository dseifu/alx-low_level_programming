#include "main.h"
#include <string.h>

/**
 * _strlen - return the length of a given sting
 *@s - the given string
 *
 * Return: the length
 */

int _strlen(char *s)
{
	char str[] = *s;
	return (strlen(str));
}
