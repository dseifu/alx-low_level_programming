#include "main.h"
#include <string.h>

/**
 * _strcpy - copy one string to another
 * @dest: destination string to be copied into
 * @src: the src string to copy from
 *
 * Return: a pointer to the copied string
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
