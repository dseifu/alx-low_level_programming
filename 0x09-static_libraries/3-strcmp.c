#include "main.h"
#include <string.h>

/**
 * _strcmp - compare two strings
 * @s1: the first string
 * @s2: the second string
 *
 * Return: 0 if identical pos if greater and neg is less
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
