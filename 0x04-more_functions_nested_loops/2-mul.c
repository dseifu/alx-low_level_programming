#include "main.h"

/**
 * mul - return the multiplication of two numbers
 * @a: the first number to be multiplied
 * @b: the second number to be multiplied
 *
 * Return: The multiplication of the two number
 */

int mul(int a, int b)
{
	long int m;

	m = a * b;
	return (m);
}
